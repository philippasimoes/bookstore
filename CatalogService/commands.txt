//MYSQL
wsl.exe docker run -p 3307:3306 --name bookstore-db --net bookstore-net -e MYSQL_ROOT_PASSWORD=root -e MYSQL_DATABASE=bookstore-db -d mysql:latest

//Catalog Service
docker run -p 8080:8080 --name catalog-service --net bookstore-net -e MYSQL_HOST=bookstore-db -e MYSQL_USER=root -e MYSQL_PASSWORD=root -e MYSQL_PORT=3307 catalog-service

//Network
docker network create bookstore-net
docker network connect bookstore-net bookstore-db